[project]
name = "pump-detector"
version = "1.0.0"
description = "Real-time crypto scanner detecting early pump signals (Gate.io / Binance)."
requires-python = ">=3.11"
dependencies = [
  "fastapi[standard]>=0.115",
  "httpx>=0.27",
]
